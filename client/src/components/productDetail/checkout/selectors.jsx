import React from 'react';

class Selectors extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>
        Selectors

      </div>
    );
  }
}

export default Selectors;
